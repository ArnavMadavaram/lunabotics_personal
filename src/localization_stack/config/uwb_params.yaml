uwb_correction_node:
  ros__parameters:
    # Gaussian ranging noise (metres) — target < 0.10 m per CDR spec
    noise_sigma: 0.05

    # Fire a correction if the robot has been running longer than this (seconds)
    # Simulates post-excavation correction trigger
    uptime_trigger: 30.0
